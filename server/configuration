[common]
# set logfilename if need log to file
#log_filename = log
log_level = DEBUG
api_version = 1
redis_ip = 127.0.0.1

[model]
# sqlite3, mysql
db_type = sqlite3
# only for sqlite
db_filename = hms

